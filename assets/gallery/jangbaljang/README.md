# @_jangbaljang_

Folder renamed from `_jangbaljang_` to `jangbaljang` because GitHub Pages'
default Jekyll build silently skips any directory starting with `_`.
The site still credits this photographer as **@_jangbaljang_** — handle
display and IG link are mapped back via `handleOf()` in `gallery.html` /
`index.html`.

- Instagram: <https://www.instagram.com/_jangbaljang_/>
